/** Path the site is served under on GitHub Pages. next/link adds it by itself; plain URLs need it. */
export const BASE_PATH = "/portugal-explicado";

/** Absolute address of the site, for the RSS feed and metadata. */
export const SITE_URL = `https://joaosfvaz.github.io${BASE_PATH}`;
